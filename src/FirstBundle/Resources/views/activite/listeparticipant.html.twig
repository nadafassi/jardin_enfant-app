{% extends 'back.html.twig' %}

{% block nav %}
    <div class="header bg-gradient-primary pb-8 pt-5 pt-md-8">
        <div class="container-fluid">
            <center><h1>Liste des participants</h1></center>
        </div></div>
{% endblock nav %}
{% block body %}
    <center>

        <table id="example" class="table table-striped table-bordered" style="width:80% ">
            <tbody>
            <tr>


                <th>Nom de l'enfant</th>
                <th>Prenom de l'enfant</th>
                <th>Date de l'activité</th>

            </tr>

            {% for activite in enfants %}
            <tr>



                <td>{{ activite.enfant.nom }}</td>
                <td>{{ activite.enfant.prenom }}</td>
                <td>{{ activite.Date|date('Y-m-d ') }}</td>
            </tr>
            {% endfor %}

            </tbody>
        </table>

        <li>
            <a href="{{ path('activite_indexback') }}">Back to the list</a>
        </li>

    </center>
{% endblock %}
